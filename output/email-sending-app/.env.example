# Email Sending app
NODE_ENV=development
PORT=3002
# If you want to send real emails via SMTP, set these. Otherwise emails are logged.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
# Optionally configure path to Google Sheets credentials if you want to persist to Sheets
GOOGLE_SHEETS_CREDENTIALS=
GOOGLE_SHEETS_TOKEN=
# Optional: path to a Google service account key JSON. If provided, the service account will be used for Sheets operations
GOOGLE_SERVICE_ACCOUNT_KEY=
